Two GreetingService implementation are there.

I18nEnglishGreetingService is annotated @Profile("EN")

I18nHindiGreetingService is annotated @Profile("HI")

When we set profile in application.properties, that Service will be injected.

